package com.builpr.search.solr;

/**
 * @author Alexander Zeitler
 *         Enum containing every aspect that a search query can be sorted by.
 */
public enum SOLR_SORT {
    RELEVANCE, title, rating, numberOfDownloads, uploadDate
}
